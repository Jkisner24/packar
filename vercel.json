{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "scripts": {
            "build": "npm run prisma:generate && npm run build"
          }
        }
      }
    ]
  }
  